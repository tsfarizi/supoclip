{
  "name": "supoclip-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E Playwright suite for SupoClip against the native stack (run.ps1)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "~1.55.0"
  }
}
